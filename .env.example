# Без Docker: сначала запустите `npm run db:dev`, затем `npm run db:setup`.
DATABASE_URL="postgres://postgres:postgres@127.0.0.1:51214/template1?sslmode=disable&connection_limit=10&connect_timeout=0&max_idle_connection_lifetime=0&pool_timeout=0&socket_timeout=0"

# Если захотите вернуться к Docker, можно заменить DATABASE_URL на:
# DATABASE_URL="postgresql://telegram:telegram@localhost:5432/telegram_mini_app?schema=public"
TELEGRAM_BOT_TOKEN="123456789:replace-with-botfather-token"
NEXT_PUBLIC_APP_URL="https://your-public-mini-app-url.example"
